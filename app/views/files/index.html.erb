<h1>Listing files</h1>

<table class="records">
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Manufacturer</th>
    <th>Product class</th>
    <th>Software version</th>
    <th></th>
  </tr>

<% @files.each do |file| %>
  <tr>
    <td><%= file['filename'] %></td>
    <td><%= file['metadata']['FileType'] rescue '' %></td>
    <td><%= file['metadata']['Manufacturer'] rescue '' %></td>
    <td><%= file['metadata']['ProductClass'] rescue '' %></td>
    <td><%= file['metadata']['SoftwareVersion'] rescue '' %></td>
    <td><%= link_to 'Destroy', "/files/#{file['filename']}", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New File', '/files/new' %>
